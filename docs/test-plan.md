# Test Plan - She-Na Platform

## Test Cases Summary

| Section | Risk Area | Test IDs |
|---|---|---|
| Public Home Page | Navbar anchors, public modals, public content | HP-003 - HP-005, TC-001 - TC-005 |
| Participant Events and Suggestions | Event registration, capacity rules, workshop suggestions | TC-006 - TC-011 |
| Appointments and Profile | Appointment booking, duplicate prevention, profile updates, password validation | TC-012, TC-016, TC-019 |
| Admin Management | Event management, role changes, audit trail, user search | TC-013 - TC-015, TC-018 |

## Public Home Page

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| HP-003 | Verify that navbar internal section links work | User is on `/public`. | 1. Open `/public` at the top of the page.<br>2. Click the Home link and confirm the page targets the hero area.<br>3. Click the About link and confirm the page targets the About section.<br>4. Click the Support link and confirm the page targets the Support Areas section.<br>5. Click the Stories link and confirm the page targets the Stories Preview section.<br>6. Click the Donations link and confirm the page targets the Donation CTA section.<br>7. Click the Contact link and confirm the page targets the Contact area. | `#home`, `#about`, `#support`, `#stories`, `#donate`, `#contact` | Each link should point to an existing public Home Page section. | The navbar links point to existing anchors: `#home`, `#about`, `#support`, `#stories`, `#donate`, and `#contact`. These sections are implemented on the Home Page. | Pass |
| HP-004 | Verify that the Join button opens the Join Community modal | User is on `/public`. | 1. Open `/public`.<br>2. Click the navbar Join button labeled `להצטרף`.<br>3. Confirm that the page does not navigate away from `/public`.<br>4. Confirm that the Join Community modal appears above the Home Page.<br>5. Verify that modal content is visible and the close control is available.<br>6. Close the modal and confirm the Home Page is usable again. | Button: `להצטרף` | The Join Community modal should open and be closable without leaving the Home Page. | The Join button prevents default navigation and opens `JoinCommunityModal`. The modal includes close behavior and Escape-key close handling. | Pass |
| HP-005 | Verify that the Volunteer button opens the Volunteer modal | User is on `/public`. | 1. Open `/public`.<br>2. Click the navbar Volunteer button labeled `להתנדב`.<br>3. Confirm that the page does not navigate away from `/public`.<br>4. Confirm that the Volunteer modal appears above the Home Page.<br>5. Verify that modal content is visible and the close control is available.<br>6. Close the modal and confirm the Home Page is usable again. | Button: `להתנדב` | The Volunteer modal should open and be closable without leaving the Home Page. | The implementation wires the Volunteer button to `VolunteerModal` and includes close and Escape-key handling. Marked failed from manual testing, so this should be retested in the browser to identify the runtime/UI issue. | Fail |

## Public Website and Join Form

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-001 | Validate Join Community form required fields | User is on the public home page at `/public`. | 1. Open the Join Community modal.<br>2. Leave all required fields empty.<br>3. Click Submit. | Empty form. | Form is not submitted. Required-field validation messages appear for join approval, full name, email, phone, and consent. User remains in the modal. | The form stayed open and displayed validation messages for the required fields. No join request was submitted while the required information was missing. | Pass |
| TC-002 | Submit a valid Join Community request | User is on `/public`; Firebase connection is available. | 1. Open the Join Community modal.<br>2. Fill all required fields with valid values.<br>3. Select consent option.<br>4. Click Submit. | Full name: Test Participant; email: `test.participant@example.com`; phone: valid Israeli phone; consent: Yes. | Join request is saved successfully. Success message is displayed. Form values are cleared or modal can be closed without duplicate submission. | The valid join request was saved to the `joinRequests` collection with `source: public-website`, `status: new`, and `createdAt`. A success state was displayed and the form was reset. | Pass |
| TC-003 | Reject invalid Join Community email and phone | User is on `/public` and Join Community modal is open. | 1. Enter valid name and consent.<br>2. Enter invalid email format.<br>3. Enter an incomplete phone number.<br>4. Click Submit. | Email: `bad-email`; phone: `123`. | Form is not submitted. Email and phone validation messages are shown. No join request is created in Firestore. | The form rejected the invalid email and incomplete phone number, displayed field-level validation errors, and did not create a Firestore join request. | Pass |
| TC-004 | Reject future birth date in Join Community form | User is on `/public` and Join Community modal is open. | 1. Fill required fields with valid data.<br>2. Enter or select a birth date after today's date.<br>3. Click Submit. | Birth date: future date, for example `01/01/2030`. | Form is not submitted. Birth date validation indicates that a future date is not allowed. | The birth date field rejected a future date and displayed a validation message. The form stayed open and no join request was submitted. | Pass |
| TC-005 | Display published public content correctly | Public content and published events/articles exist in Firestore. | 1. Open `/public`.<br>2. Review hero, about, statistics, articles, events preview, team, donation, contact, and footer sections.<br>3. Scroll through the page. | Existing published public content records. | Page loads without console errors. Published content is displayed in the correct sections. Empty or loading states appear only when data is unavailable. Layout remains readable while scrolling. | The public home page displayed the implemented hero, about, support, stories/articles, donation, contact, and footer sections. Content and fallback states rendered in the correct areas and the page remained readable while scrolling. | Pass |

## Participant Events and Suggestions

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-006 | Register participant for an available event | Participant user is already signed in; Firebase is connected; Firestore has at least one document in `events` with `status: published` and available capacity. | User view:<br>1. Open `/events` in the app.<br>2. Locate an available event, for example `Yoga Workshop`.<br>3. Click Register Now.<br>4. Check that the event card changes to Registered.<br><br>Behind the scenes:<br>5. Open Firebase Firestore.<br>6. Check `events/{eventId}/registrations/{uid}`.<br>7. Check `users/{uid}/registrations/{eventId}`.<br>8. Refresh `/events` and confirm the count uses the registration subcollection count. | Existing Firestore event in `events/{eventId}`:<br>`title: Yoga Workshop`, `status: published`, `maxParticipants: 20`, valid `startTime`, `location`.<br>Before test: `events/{eventId}/registrations` has `8` docs.<br>Signed-in participant: UID `<uid>`, email `participant@test.com`, display name `Test Participant`. | User view: Register Now changes to a loading state, then the event shows Registered. The visible count changes from `8 / 20` to `9 / 20`.<br><br>Behind the scenes: Firestore creates `events/{eventId}/registrations/{uid}` with `userId`, `userName`, `userEmail`, `status: confirmed`, `checkedIn: false`, `registeredAt`, `participantName`, and `participantEmail`. Firestore also creates `users/{uid}/registrations/{eventId}` with `eventId`, `eventTitle`, `eventDate`, `eventLocation`, `status: confirmed`, `checkedIn: false`, and `registeredAt`. | The participant successfully registered. The event card changed to Registered, the count increased from `8 / 20` to `9 / 20`, and both Firestore mirror documents were created with the expected registration fields. | Pass |
| TC-007 | Cancel participant event registration | Participant user is already signed in; Firebase is connected; the participant already has registration mirror documents for a published event. | User view:<br>1. Open `/events` in the app.<br>2. Locate an event marked Registered.<br>3. Click Cancel Registration.<br>4. Check that the event card returns to the unregistered state.<br><br>Behind the scenes:<br>5. Open Firebase Firestore.<br>6. Confirm `events/{eventId}/registrations/{uid}` was deleted.<br>7. Confirm `users/{uid}/registrations/{eventId}` was deleted.<br>8. Refresh `/events` and confirm the count decreased. | Existing Firestore event in `events/{eventId}`:<br>`title: Yoga Workshop`, `status: published`, `maxParticipants: 20`.<br>Before test: `events/{eventId}/registrations` has `9` docs.<br>Existing docs: `events/{eventId}/registrations/{uid}` and `users/{uid}/registrations/{eventId}`.<br>Expected count after test: `8`. | User view: Cancel Registration changes to a loading state, then the event no longer shows Registered. The action returns to Register Now if spots remain. The visible count changes from `9 / 20` to `8 / 20`.<br><br>Behind the scenes: Both Firestore mirror documents are deleted. The count is read from `events/{eventId}/registrations`, so it decreases by 1 and does not go below 0. | The participant successfully cancelled the registration. The card returned to Register Now, the count decreased from `9 / 20` to `8 / 20`, and both Firestore mirror documents were deleted. | Pass |
| TC-008 | Prevent registration when event is full | Participant user is already signed in; Firebase is connected; Firestore has a published event where the number of documents in `events/{eventId}/registrations` equals `maxParticipants`. | User view:<br>1. Open `/events` in the app.<br>2. Locate a full event, for example `Art Therapy Circle`.<br>3. Check the action button on the event card.<br>4. Try to click the button if it is available.<br><br>Behind the scenes:<br>5. Open Firebase Firestore.<br>6. Confirm no new document is created under `events/{eventId}/registrations` for the current user.<br>7. Confirm no new document is created under `users/{uid}/registrations/{eventId}`. | Existing Firestore event in `events/{eventId}`:<br>`title: Art Therapy Circle`, `status: published`, `maxParticipants: 10`.<br>Before test: `events/{eventId}/registrations` has `10` docs.<br>Signed-in participant: UID `<uid>`, email `participant@test.com`. | User view: The event displays Fully Booked and the registration action is disabled. The participant cannot register for the full event.<br><br>Behind the scenes: Firestore registration count stays `10`. No document is created at `events/{eventId}/registrations/{uid}` or `users/{uid}/registrations/{eventId}`. | The full event displayed Fully Booked and the action was disabled. Firestore registration count stayed at `10`, and no new event or user registration document was created. | Pass |
| TC-009 | Validate workshop suggestion required fields | Participant user is already signed in; participant is on the `/events` page; suggestion modal can be opened. | User view:<br>1. Click Suggest a Workshop.<br>2. Leave Workshop title empty.<br>3. Leave Category unselected.<br>4. Leave Short description empty.<br>5. Click Submit Suggestion.<br><br>Behind the scenes:<br>6. Open Firebase Firestore.<br>7. Confirm no new document was created in `workshop_suggestions`. | Empty title.<br>Empty category.<br>Empty description.<br>Optional reason can be empty.<br>Anonymous can be checked or unchecked. | User view: The modal stays open. Validation messages appear for title, category, and description. The participant can correct the fields and submit again.<br><br>Behind the scenes: Firestore does not create a document in `workshop_suggestions` because required data is missing. | The suggestion modal stayed open and displayed validation errors for title, category, and description. No document was created in `workshop_suggestions`. | Pass |
| TC-010 | Submit valid workshop suggestion | Participant user is already signed in; Firebase is connected; participant is on the `/events` page. | User view:<br>1. Click Suggest a Workshop.<br>2. Fill in title, category, description, and optional reason.<br>3. Choose whether to submit anonymously.<br>4. Click Submit Suggestion.<br>5. Check that a success message appears.<br><br>Behind the scenes:<br>6. Open Firebase Firestore.<br>7. Check the `workshop_suggestions` collection.<br>8. Confirm a new document was created with the same form values. | Suggestion title: `Anxiety support circle`.<br>Category: `Anxiety Support`.<br>Description: `A weekly support group for stress and anxiety`.<br>Reason: `It can help participants share coping tools`.<br>Anonymous: test once with `false` and once with `true` if possible.<br>Signed-in participant UID: `<uid>`. | User view: Submit button shows a submitting state. Success message appears. Form resets and the modal closes after confirmation.<br><br>Behind the scenes: Firestore creates a document in `workshop_suggestions` with `title`, `category`, `description`, `reason`, `anonymous`, `createdAt`, and `status: new`. If `anonymous` is `false`, `userId` equals the signed-in user's UID. If `anonymous` is `true`, `userId` is `null`. The current code does not save participant email in this document. | The valid workshop suggestion was submitted successfully. A success message appeared, the form reset, the modal closed after confirmation, and a new `workshop_suggestions` document was created with the submitted values and `status: new`. | Pass |
| TC-011 | Prevent overbooking when event becomes full after page load | Participant user is already signed in; Firebase is connected; user loaded `/events` while the event still had one available spot. Another participant registers before the current user clicks Register Now. | User view:<br>1. Open `/events` and locate an event showing `9 / 10` registered.<br>2. In Firestore or another browser session, add one more registration so the event reaches `10 / 10`.<br>3. Return to the first browser without refreshing.<br>4. Click Register Now on the stale event card.<br><br>Behind the scenes:<br>5. Check `events/{eventId}/registrations`.<br>6. Check `users/{uid}/registrations/{eventId}`.<br>7. Refresh `/events` and review the registration count. | Existing Firestore event in `events/{eventId}`:<br>`title: Capacity Race Test`<br>`status: published`<br>`maxParticipants: 10`.<br>Initial count visible to user: `9`.<br>Actual count before clicking: `10`.<br>Signed-in participant: UID `<uid>`, email `participant@test.com`. | The system should re-check capacity at registration time and block the stale registration. The count should stay `10 / 10`, and no documents should be created for the current user. | The stale Register Now action was still allowed. `addRegistration` writes the registration mirror documents without checking `maxParticipants` or the current server-side registration count, so the event can become overbooked at `11 / 10`. | Fail |

## Appointments and Profile

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-012 | Update personal details and verify Firestore update | Participant user is signed in and already has existing profile data stored in Firestore. | User view:<br>1. Sign in using a valid Participant account.<br>2. Open the `/profile` page.<br>3. Verify that the current user data is loaded correctly inside the Personal Details section.<br>4. Open Firebase Console and navigate to Firestore Database.<br>5. Open the `users` collection and review the current profile values before editing.<br>6. Return to the profile page and click Edit Profile.<br>7. Update the full name in the Full Name field.<br>8. Update the phone number in the Phone Number field.<br>9. Update the city and street address in the City and Street Address fields.<br>10. Change the birth date using the Birth Date field.<br>11. Change the preferred contact method from the Preferred Contact Method dropdown.<br>12. Change the language from the Language dropdown.<br>13. Click Save Changes.<br><br>Behind the scenes:<br>14. Return to Firestore and refresh the user document.<br>15. Verify that the user data has been updated with the new values inside Firestore.<br>16. Return to the website and refresh the `/profile` page.<br>17. Verify that all updated values are still displayed correctly after reloading data from Firestore. | Full name: `Dema Test`.<br>Phone: `+972584888240`.<br>City: `Jerusalem`.<br>Address: `Al Tur`.<br>Language: `English`. | User profile data is successfully updated in Firestore, and the updated values are displayed correctly on the profile page after refreshing the page. | The user profile document in Firestore was successfully updated with the new values. After refreshing the `/profile` page, all updated information was displayed correctly without data loss. | Pass |
| TC-016 | Prevent updating the password to the current password | Participant user is signed in, knows the current password, and Firebase Authentication is working correctly. | User view:<br>1. Sign in using a valid Participant account.<br>2. Open the `/profile` page.<br>3. Scroll to the Change Password section.<br>4. Verify that the Current Password, New Password, and Confirm Password fields are displayed correctly.<br>5. Enter the current password in the Current Password field.<br>6. Enter the same current password again in the New Password field.<br>7. Enter the same password in the Confirm Password field.<br>8. Click Update Password.<br>9. Observe the message displayed to the user after attempting the update.<br><br>Behind the scenes:<br>10. Open Firebase Authentication and verify whether a password update operation was triggered or any account changes were recorded. | The current password and the new password are identical. | The system should prevent updating the password to the same current password and display a validation error message indicating that the new password must be different from the current password. | The system incorrectly allowed the password update and displayed a success message even though the user entered the same current password as the new password. The current implementation validates required fields, matching confirmation, and minimum length, but does not compare `currentPassword` with `newPassword`. | Fail |
| TC-019 | Book a new appointment and prevent booking multiple appointments at the same time | Participant user is signed in, available therapists and time slots exist, and Firestore is connected to the system. | User view:<br>1. Sign in using a valid Participant account.<br>2. Open the `/appointments` page.<br>3. Verify that appointment types, therapists, and available time slots are loaded correctly.<br>4. Select an appointment type such as Psychologist.<br>5. Select a therapist from the Select Therapist dropdown.<br>6. Select an available date from Select Date.<br>7. Select an available time from Select Time.<br>8. Enter optional notes in the Notes field.<br>9. Click Book Appointment.<br>10. Verify that a success message appears after booking.<br><br>Behind the scenes:<br>11. Open Firebase Console and navigate to Firestore Database.<br>12. Open the `appointments` collection and verify that a new appointment document was created.<br>13. Verify that the document contains the correct fields such as `participantId`, `type`, `therapistName`, `date`, `time`, `status`, and `notes`.<br>14. Return to the website and verify that the appointment appears correctly inside the My Appointments section.<br>15. Refresh the `/appointments` page and verify that the appointment still appears after reloading data from Firestore.<br>16. Attempt to create another appointment using the same date and time.<br>17. Click Book Appointment again.<br>18. Verify that an error message appears indicating that another appointment already exists at the same time.<br>19. Return to Firestore and verify that no additional appointment document was created for the duplicate booking attempt. | First appointment: Psychologist, `05/20/2026`, `5:00 PM`.<br>Second appointment: Therapist, same date and time. | The first appointment is successfully created in Firestore and appears in the appointments list. When attempting to create another appointment at the same time, the booking is blocked, an error message is displayed, and no additional document is created in Firestore. | The first appointment was successfully created in Firestore and appeared in the My Appointments section. When attempting to book another appointment at the same date and time, the system displayed an error message and prevented the duplicate booking. No additional document was created in Firestore. | Pass |

## Admin Management

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-013 | Admin creates a published event | Admin user is already signed in and authorized for `/admin/events`; Firebase is connected. | Admin view:<br>1. Open `/admin/events`.<br>2. Click Add New Event.<br>3. Fill Event Title, Category, Start Date & Time, Location, Max Participants, and Description.<br>4. Optional: fill Instructor / Therapist and End Date & Time.<br>5. Click Create Event.<br>6. Check that the event appears in the Events Management table.<br>7. Open `/events` as a participant and check that the published event appears there too.<br><br>Behind the scenes:<br>8. Open Firebase Firestore.<br>9. Check the `events` collection for the new event document.<br>10. Check the `audit_logs` collection for a `CREATE_EVENT` record. | Event data:<br>`title: Admin Test Workshop`<br>`category: Workshop`<br>`startTime: future date/time`<br>`endTime: optional future date/time`<br>`instructor: She-Na team`<br>`location: She-Na Center`<br>`description: Test event created by admin`<br>`maxParticipants: 20` | Admin view: Event is added to the admin table with title, category, start time, location, capacity, registered count, status, and action buttons. Participant view: event appears on `/events` because the code creates it with `status: published`.<br><br>Behind the scenes: Firestore creates a new document in `events` with the submitted fields, `maxParticipants` as a number, `status: published`, and `createdAt`. Firestore also creates an `audit_logs` document with `actionType: CREATE_EVENT`, `targetId` equal to the new event ID, admin email/ID, timestamp, details, summary, and `expiresAt`. | Code review confirms the create flow submits `status: published` by default, converts `maxParticipants` to a number, writes the event, refreshes the admin list, and logs `CREATE_EVENT` through `logAuditEvent`. Manual Firebase verification is still recommended for the exact document values. | Pass |
| TC-014 | Admin deletes event after confirmation | Admin user is already signed in and authorized for `/admin/events`; a test event exists in Firestore. | Admin view:<br>1. Open `/admin/events`.<br>2. Locate the test event in the table.<br>3. Click Delete.<br>4. Confirm the browser confirmation message.<br>5. Check that the event disappears from the admin table.<br>6. Open `/events` as a participant and check that the event no longer appears.<br><br>Behind the scenes:<br>7. Open Firebase Firestore.<br>8. Confirm the event document is deleted from `events/{eventId}`.<br>9. Check the `audit_logs` collection for a `DELETE_EVENT` record. | Existing event document in `events/{eventId}`:<br>`title: Admin Test Workshop`<br>`status: published`<br>`maxParticipants: 20` | Admin view: Browser asks for confirmation before deletion. After confirming, the event is removed from the admin events table. Participant view: deleted event no longer appears on `/events`.<br><br>Behind the scenes: Firestore deletes the document at `events/{eventId}`. Firestore creates an `audit_logs` document with `actionType: DELETE_EVENT`, `targetId` equal to the deleted event ID, and `details.deleted` equal to the event title. Current code deletes the event document only; it does not explicitly clean nested registration subcollections. | Code review confirms the delete flow removes the event document and logs `DELETE_EVENT` with `details.deleted` set to the event title. Nested registration subcollections are not explicitly cleaned by the client code and should be treated as a known limitation. | Pass |
| TC-015 | Admin changes user role and audit log is created | Admin user is already signed in and authorized for `/admin/roles`; target user exists in the `users` collection. | Admin view:<br>1. Open `/admin/roles`.<br>2. Locate a non-current test user in the data grid.<br>3. Change the role using the Change Role dropdown, for example from `participant` to `admin`.<br>4. Check that the Current Role chip updates in the table.<br>5. Open `/admin/audit-log`.<br>6. Filter by admin email if needed and locate the role change log.<br><br>Behind the scenes:<br>7. Open Firebase Firestore.<br>8. Check `users/{targetUserId}` and confirm the `role` field changed.<br>9. Check `audit_logs` for a `ROLE_CHANGE` record. | Target Firestore user:<br>`users/{targetUserId}` with `email: role-test@example.com`, `displayName: Role Test`, `role: participant`.<br>New role: `admin`. | Admin view: Role dropdown becomes disabled while saving. After save, the target user's role chip shows the new role. Audit Log page shows a `ROLE_CHANGE` action.<br><br>Behind the scenes: Firestore updates `users/{targetUserId}.role` to the selected role. Firestore creates an `audit_logs` document with `actionType: ROLE_CHANGE`, `targetId` equal to the target user ID, and `details.before`, `details.after`, and `details.description`. | Code review confirms `/admin/roles` disables the role dropdown while saving, updates `users/{targetUserId}.role`, updates local table state, and logs `ROLE_CHANGE` with before/after details. Manual audit-log page verification is still recommended. | Pass |
| TC-018 | Search users by name or email | Admin user is already signed in and authorized for `/admin/users`; Firebase is connected; users exist in the `users` collection. | Admin view:<br>1. Open `/admin/users`.<br>2. Wait for the User Management data grid to load.<br>3. Type part of an email in the search field.<br>4. Check the filtered results.<br>5. Clear the search field.<br>6. Type part of a display name.<br>7. Check the filtered results again.<br><br>Behind the scenes:<br>8. Open Firebase Firestore only to confirm the searched users exist in `users`.<br>9. Confirm no Firestore data is changed by searching. | Existing users in Firestore:<br>`users/{uid1}` with `email: participant@test.com`, `displayName: Test Participant`, `role: participant`.<br>`users/{uid2}` with `email: admin@test.com`, `displayName: Admin User`, `role: admin`.<br>Search examples: `participant`, `test`, `Admin`. | Admin view: Data grid filters to users whose email or display name includes the search text. Search is case-insensitive. Clearing the search field restores the loaded users list.<br><br>Behind the scenes: The page reads up to 200 documents from the `users` collection using `limit(200)`. Filtering is done in the frontend state only. No Firestore documents are created, updated, or deleted. | Search filtered the loaded user list by name, email, phone, or address using case-insensitive frontend matching. Clearing the search restored the list, and no Firestore documents were changed. | Pass |
