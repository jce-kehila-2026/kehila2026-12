# Test Plan - She-Na Platform

## Test Cases Summary

| Section | Risk Area | Test IDs |
|---|---|---|
| Public Website and Join Form | Public content, join request validation, user data submission | TC-001 - TC-005 |
| Participant Events and Suggestions | Event registration, capacity rules, workshop suggestions | TC-006 - TC-010 |
| Appointments and Profile | Appointment booking, duplicate prevention, profile validation | TC-011 - TC-012 |
| Admin Management | Event management, role changes, audit trail, user search | TC-013 - TC-016 |

## Public Website and Join Form

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-001 | Validate Join Community form required fields | User is on the public home page at `/public`. | 1. Open the Join Community modal.<br>2. Leave all required fields empty.<br>3. Click Submit. | Empty form. | Form is not submitted. Required-field validation messages appear for join approval, full name, email, phone, and consent. User remains in the modal. | TBD | TBD |
| TC-002 | Submit a valid Join Community request | User is on `/public`; Firebase connection is available. | 1. Open the Join Community modal.<br>2. Fill all required fields with valid values.<br>3. Select consent option.<br>4. Click Submit. | Full name: Test Participant; email: test.participant@example.com; phone: valid Israeli phone; consent: Yes. | Join request is saved successfully. Success message is displayed. Form values are cleared or modal can be closed without duplicate submission. | TBD | TBD |
| TC-003 | Reject invalid Join Community email and phone | User is on `/public` and Join Community modal is open. | 1. Enter valid name and consent.<br>2. Enter invalid email format.<br>3. Enter an incomplete phone number.<br>4. Click Submit. | Email: `bad-email`; phone: `123`. | Form is not submitted. Email and phone validation messages are shown. No join request is created in Firestore. | TBD | TBD |
| TC-004 | Reject future birth date in Join Community form | User is on `/public` and Join Community modal is open. | 1. Fill required fields with valid data.<br>2. Enter or select a birth date after today's date.<br>3. Click Submit. | Birth date: future date, for example `01/01/2030`. | Form is not submitted. Birth date validation indicates that a future date is not allowed. | TBD | TBD |
| TC-005 | Display published public content correctly | Public content and published events/articles exist in Firestore. | 1. Open `/public`.<br>2. Review hero, about, statistics, articles, events preview, team, donation, contact, and footer sections.<br>3. Scroll through the page. | Existing published public content records. | Page loads without console errors. Published content is displayed in the correct sections. Empty or loading states appear only when data is unavailable. Layout remains readable while scrolling. | TBD | TBD |

## Participant Events and Suggestions

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-006 | Register participant for an available event | Participant user is already signed in; Firebase is connected; Firestore has at least one document in `events` with `status: published` and available capacity. | User view:<br>1. Open `/events` in the app.<br>2. Locate an available event, for example `Yoga Workshop`.<br>3. Click Register Now.<br>4. Check that the event card changes to Registered.<br><br>Behind the scenes:<br>5. Open Firebase Firestore.<br>6. Check `events/{eventId}/registrations/{uid}`.<br>7. Check `users/{uid}/registrations/{eventId}`.<br>8. Refresh `/events` and confirm the count uses the registration subcollection count. | Existing Firestore event in `events/{eventId}`:<br>`title: Yoga Workshop`, `status: published`, `maxParticipants: 20`, valid `startTime`, `location`.<br>Before test: `events/{eventId}/registrations` has `8` docs.<br>Signed-in participant: UID `<uid>`, email `participant@test.com`, display name `Test Participant`. | User view: Register Now changes to a loading state, then the event shows Registered. The visible count changes from `8 / 20` to `9 / 20`.<br><br>Behind the scenes: Firestore creates `events/{eventId}/registrations/{uid}` with `userId`, `userName`, `userEmail`, `status: confirmed`, `checkedIn: false`, `registeredAt`, `participantName`, and `participantEmail`. Firestore also creates `users/{uid}/registrations/{eventId}` with `eventId`, `eventTitle`, `eventDate`, `eventLocation`, `status: confirmed`, `checkedIn: false`, and `registeredAt`. | TBD | TBD |
| TC-008 | Prevent registration when event is full | Participant user is already signed in; Firebase is connected; Firestore has a published event where the number of documents in `events/{eventId}/registrations` equals `maxParticipants`. | User view:<br>1. Open `/events` in the app.<br>2. Locate a full event, for example `Art Therapy Circle`.<br>3. Check the action button on the event card.<br>4. Try to click the button if it is available.<br><br>Behind the scenes:<br>5. Open Firebase Firestore.<br>6. Confirm no new document is created under `events/{eventId}/registrations` for the current user.<br>7. Confirm no new document is created under `users/{uid}/registrations/{eventId}`. | Existing Firestore event in `events/{eventId}`:<br>`title: Art Therapy Circle`, `status: published`, `maxParticipants: 10`.<br>Before test: `events/{eventId}/registrations` has `10` docs.<br>Signed-in participant: UID `<uid>`, email `participant@test.com`. | User view: The event displays Fully Booked and the registration action is disabled. The participant cannot register for the full event.<br><br>Behind the scenes: Firestore registration count stays `10`. No document is created at `events/{eventId}/registrations/{uid}` or `users/{uid}/registrations/{eventId}`. | TBD | TBD |
| TC-007 | Cancel participant event registration | Participant user is already signed in; Firebase is connected; the participant already has registration mirror documents for a published event. | User view:<br>1. Open `/events` in the app.<br>2. Locate an event marked Registered.<br>3. Click Cancel Registration.<br>4. Check that the event card returns to the unregistered state.<br><br>Behind the scenes:<br>5. Open Firebase Firestore.<br>6. Confirm `events/{eventId}/registrations/{uid}` was deleted.<br>7. Confirm `users/{uid}/registrations/{eventId}` was deleted.<br>8. Refresh `/events` and confirm the count decreased. | Existing Firestore event in `events/{eventId}`:<br>`title: Yoga Workshop`, `status: published`, `maxParticipants: 20`.<br>Before test: `events/{eventId}/registrations` has `9` docs.<br>Existing docs: `events/{eventId}/registrations/{uid}` and `users/{uid}/registrations/{eventId}`.<br>Expected count after test: `8`. | User view: Cancel Registration changes to a loading state, then the event no longer shows Registered. The action returns to Register Now if spots remain. The visible count changes from `9 / 20` to `8 / 20`.<br><br>Behind the scenes: Both Firestore mirror documents are deleted. The count is read from `events/{eventId}/registrations`, so it decreases by 1 and does not go below 0. | TBD | TBD |
| TC-010 | Submit valid workshop suggestion | Participant user is already signed in; Firebase is connected; participant is on the `/events` page. | User view:<br>1. Click Suggest a Workshop.<br>2. Fill in title, category, description, and optional reason.<br>3. Choose whether to submit anonymously.<br>4. Click Submit Suggestion.<br>5. Check that a success message appears.<br><br>Behind the scenes:<br>6. Open Firebase Firestore.<br>7. Check the `workshop_suggestions` collection.<br>8. Confirm a new document was created with the same form values. | Suggestion title: `Anxiety support circle`.<br>Category: `Anxiety Support`.<br>Description: `A weekly support group for stress and anxiety`.<br>Reason: `It can help participants share coping tools`.<br>Anonymous: test once with `false` and once with `true` if possible.<br>Signed-in participant UID: `<uid>`. | User view: Submit button shows a submitting state. Success message appears. Form resets and the modal closes after confirmation.<br><br>Behind the scenes: Firestore creates a document in `workshop_suggestions` with `title`, `category`, `description`, `reason`, `anonymous`, `createdAt`, and `status: new`. If `anonymous` is `false`, `userId` equals the signed-in user's UID. If `anonymous` is `true`, `userId` is `null`. The current code does not save participant email in this document. | TBD | TBD |
| TC-009 | Validate workshop suggestion required fields | Participant user is already signed in; participant is on the `/events` page; suggestion modal can be opened. | User view:<br>1. Click Suggest a Workshop.<br>2. Leave Workshop title empty.<br>3. Leave Category unselected.<br>4. Leave Short description empty.<br>5. Click Submit Suggestion.<br><br>Behind the scenes:<br>6. Open Firebase Firestore.<br>7. Confirm no new document was created in `workshop_suggestions`. | Empty title.<br>Empty category.<br>Empty description.<br>Optional reason can be empty.<br>Anonymous can be checked or unchecked. | User view: The modal stays open. Validation messages appear for title, category, and description. The participant can correct the fields and submit again.<br><br>Behind the scenes: Firestore does not create a document in `workshop_suggestions` because required data is missing. | TBD | TBD |

## Appointments and Profile

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-011 | Book appointment and prevent duplicate booking | Participant user is already signed in; appointment type and therapist data are available. | 1. Open `/appointments`.<br>2. Select an appointment type.<br>3. Select a therapist, date, time, and optional notes.<br>4. Click Book Appointment.<br>5. Try booking the same therapist/date/time again. | Type: wellness appointment; therapist: available therapist; date: valid date; time: available slot. | First booking is created with pending status and success message appears. Duplicate booking attempt is blocked with an error that the time is already booked. | TBD | TBD |
| TC-012 | Validate profile personal details before saving | Participant user is already signed in and on `/profile`; profile edit mode is available. | 1. Enter edit mode.<br>2. Clear required fields or enter invalid email/phone.<br>3. Click Save Changes. | Full name empty; email: `wrong-email`; phone: short number; city empty; street address empty. | Profile is not saved. Field-specific validation messages appear. Existing saved profile data is not overwritten by invalid values. | TBD | TBD |

## Admin Management

| Test ID | Test description | Pre-Condition | Test Steps | Test Data | Expected Results | Actual Results | Pass / Fail |
|---|---|---|---|---|---|---|---|
| TC-013 | Admin creates a published event | Admin user is already signed in and authorized for `/admin/events`; Firebase is connected. | Admin view:<br>1. Open `/admin/events`.<br>2. Click Add New Event.<br>3. Fill Event Title, Category, Start Date & Time, Location, Max Participants, and Description.<br>4. Optional: fill Instructor / Therapist and End Date & Time.<br>5. Click Create Event.<br>6. Check that the event appears in the Events Management table.<br>7. Open `/events` as a participant and check that the published event appears there too.<br><br>Behind the scenes:<br>8. Open Firebase Firestore.<br>9. Check the `events` collection for the new event document.<br>10. Check the `audit_logs` collection for a `CREATE_EVENT` record. | Event data:<br>`title: Admin Test Workshop`<br>`category: Workshop`<br>`startTime: future date/time`<br>`endTime: optional future date/time`<br>`instructor: She-Na team`<br>`location: She-Na Center`<br>`description: Test event created by admin`<br>`maxParticipants: 20` | Admin view: Event is added to the admin table with title, category, start time, location, capacity, registered count, status, and action buttons. Participant view: event appears on `/events` because the code creates it with `status: published`.<br><br>Behind the scenes: Firestore creates a new document in `events` with the submitted fields, `maxParticipants` as a number, `status: published`, and `createdAt`. Firestore also creates an `audit_logs` document with `actionType: CREATE_EVENT`, `targetId` equal to the new event ID, admin email/ID, timestamp, details, summary, and `expiresAt`. | TBD | TBD |
| TC-014 | Admin deletes event after confirmation | Admin user is already signed in and authorized for `/admin/events`; a test event exists in Firestore. | Admin view:<br>1. Open `/admin/events`.<br>2. Locate the test event in the table.<br>3. Click Delete.<br>4. Confirm the browser confirmation message.<br>5. Check that the event disappears from the admin table.<br>6. Open `/events` as a participant and check that the event no longer appears.<br><br>Behind the scenes:<br>7. Open Firebase Firestore.<br>8. Confirm the event document is deleted from `events/{eventId}`.<br>9. Check the `audit_logs` collection for a `DELETE_EVENT` record. | Existing event document in `events/{eventId}`:<br>`title: Admin Test Workshop`<br>`status: published`<br>`maxParticipants: 20` | Admin view: Browser asks for confirmation before deletion. After confirming, the event is removed from the admin events table. Participant view: deleted event no longer appears on `/events`.<br><br>Behind the scenes: Firestore deletes the document at `events/{eventId}`. Firestore creates an `audit_logs` document with `actionType: DELETE_EVENT`, `targetId` equal to the deleted event ID, and `details.deleted` equal to the event title. Current code deletes the event document only; it does not explicitly clean nested registration subcollections. | TBD | TBD |
| TC-015 | Admin changes user role and audit log is created | Admin user is already signed in and authorized for `/admin/roles`; target user exists in the `users` collection. | Admin view:<br>1. Open `/admin/roles`.<br>2. Locate a non-current test user in the data grid.<br>3. Change the role using the Change Role dropdown, for example from `participant` to `admin`.<br>4. Check that the Current Role chip updates in the table.<br>5. Open `/admin/audit-log`.<br>6. Filter by admin email if needed and locate the role change log.<br><br>Behind the scenes:<br>7. Open Firebase Firestore.<br>8. Check `users/{targetUserId}` and confirm the `role` field changed.<br>9. Check `audit_logs` for a `ROLE_CHANGE` record. | Target Firestore user:<br>`users/{targetUserId}` with `email: role-test@example.com`, `displayName: Role Test`, `role: participant`.<br>New role: `admin`. | Admin view: Role dropdown becomes disabled while saving. After save, the target user's role chip shows the new role. Audit Log page shows a `ROLE_CHANGE` action.<br><br>Behind the scenes: Firestore updates `users/{targetUserId}.role` to the selected role. Firestore creates an `audit_logs` document with `actionType: ROLE_CHANGE`, `targetId` equal to the target user ID, and `details.before`, `details.after`, and `details.description`. | TBD | TBD |
| TC-016 | Search users by name or email | Admin user is already signed in and authorized for `/admin/users`; Firebase is connected; users exist in the `users` collection. | Admin view:<br>1. Open `/admin/users`.<br>2. Wait for the User Management data grid to load.<br>3. Type part of an email in the search field.<br>4. Check the filtered results.<br>5. Clear the search field.<br>6. Type part of a display name.<br>7. Check the filtered results again.<br><br>Behind the scenes:<br>8. Open Firebase Firestore only to confirm the searched users exist in `users`.<br>9. Confirm no Firestore data is changed by searching. | Existing users in Firestore:<br>`users/{uid1}` with `email: participant@test.com`, `displayName: Test Participant`, `role: participant`.<br>`users/{uid2}` with `email: admin@test.com`, `displayName: Admin User`, `role: admin`.<br>Search examples: `participant`, `test`, `Admin`. | Admin view: Data grid filters to users whose email or display name includes the search text. Search is case-insensitive. Clearing the search field restores the loaded users list.<br><br>Behind the scenes: The page reads up to 200 documents from the `users` collection using `limit(200)`. Filtering is done in the frontend state only. No Firestore documents are created, updated, or deleted. | TBD | TBD |
